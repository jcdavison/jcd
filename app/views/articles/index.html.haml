= render 'layouts/blog_nav'
- if check_blog_editor_status == true 
  = render 'layouts/admin_nav'
%section#blog_content
  %section.article
    - @article.each do |article|
      %article.title #{ link_to article.title, article_path(article.id)} | #{article.created_at.to_date}